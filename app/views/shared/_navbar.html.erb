<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <span class="navbar-brand px-3">Tasks Tracker</span>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <%= link_to 'Home', home_index_path, class:"nav-item nav-link" %>
      <%= link_to 'About', home_about_path, class:"nav-item nav-link" %>
      <%= link_to 'My Tasks', tasks_path, class:"nav-item nav-link" %>
      <% if current_user %>
        <%= link_to 'Edit Account', edit_user_registration_path, class:"nav-item nav-link" %>
      <% end %>

    </div>
  </div>
  <% if current_user %>
    <%= button_to(
          "Log Out",
          destroy_user_session_path,
          method: :delete,
          class: 'btn btn-outline-danger mx-3',
          ) %>

<%else %>
    <%= link_to "Sign In", new_user_session_path, class: 'btn btn-outline-success mx-3' %>
  <% end %>

</nav>